import { Session, SupabaseClient } from '@supabase/auth-helpers-react'

export default function Profile({ session, supabaseClient }: { session: Session, supabaseClient: SupabaseClient }) {
  return(<div></div>);
}